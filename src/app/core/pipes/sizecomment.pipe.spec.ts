import { SizecommentPipe } from './sizecomment.pipe';

describe('SizecommentPipe', () => {
  it('create an instance', () => {
    const pipe = new SizecommentPipe();
    expect(pipe).toBeTruthy();
  });
});
